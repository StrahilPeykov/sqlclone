You can manipulate a table by selecting only certain columns or certain rows, or both.

- Projection: select a subset of columns from the table.
- Filtering: select a subset of rows matching given conditions.

The result remains a table, enabling further operations and queries on the reduced data.

