SQL powers most relational databases. A typical query:

```sql
SELECT *
FROM TechCompanies;
```

Whitespace doesn’t affect semantics; keywords are commonly written in uppercase. SQL is standardized by ANSI/ISO, with minor dialect differences across DBMSs.

