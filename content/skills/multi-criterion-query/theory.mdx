Compose complex WHERE clauses, compute values, and order results.

Example:

```sql
SELECT company_name, salary
FROM positions
WHERE country = 'United States' AND salary > 120000
ORDER BY salary DESC;
```

