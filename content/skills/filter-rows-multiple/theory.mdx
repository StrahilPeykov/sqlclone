Combine multiple conditions in WHERE using AND, OR, and NOT.

Examples:

```sql
SELECT *
FROM positions
WHERE country = 'United States' AND salary > 120000;
```

```sql
SELECT *
FROM companies
WHERE country = 'Netherlands' OR country = 'United Kingdom';
```

