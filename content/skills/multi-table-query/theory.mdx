Combine joins with grouping, filtering, and sorting across multiple tables.

Example:

```sql
SELECT c.country, COUNT(*) AS total
FROM positions p
JOIN companies c ON p.company_id = c.id
GROUP BY c.country;
```

