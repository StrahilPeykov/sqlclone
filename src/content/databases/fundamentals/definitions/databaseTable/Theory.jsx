import { TheoryWarning } from 'components'

export function Theory() {
	return <>
		<TheoryWarning />
		<p>When a database consists of tables (it is a relational database) these tables are called relations. It has columns known as attributes and it has rows known as tuples. A single element in the database is called a cell or a field. The database design is called the schema, and the data is the instance.</p>
	</>
}
