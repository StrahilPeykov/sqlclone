import { TheoryWarning } from 'components'

export function Theory() {
	return <>
		<TheoryWarning />
		<p>In relational databases, fields can be numbers, text, dates or more. So there are different data types. Numbers can even be of different types. Data types are determined per column. SQL also has the "NULL" value which means "empty" or "unknown".</p>
	</>
}
