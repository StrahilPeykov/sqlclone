import { TheoryWarning } from 'components'

export function Theory() {
	return <>
		<TheoryWarning />
		<p>Sometimes you want to calculate a quantity (like a min/max/average) for various groups within your table. This is what aggregation means: making groups within your table and squashing each group into a single value (or set of values).</p>
	</>
}
