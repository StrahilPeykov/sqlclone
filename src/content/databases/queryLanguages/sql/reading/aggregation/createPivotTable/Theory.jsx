import { TheoryWarning } from 'components'

export function Theory() {
	return <>
		<TheoryWarning />
		<p>Using aggregation, you can easily make pivot tables. For this, first set up the table you wish to aggregate on. Then apply the aggregation to create the key-value table. Then apply the SQL pivot command to set up the pivot table.</p>
	</>
}
